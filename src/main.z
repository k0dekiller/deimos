use * from cwio
use utime
use Pin from machine
use sys
use os

init!

screen.on!
screen.init!
keyboard.init!

keyboard.wait_for(KEYBOARD.KEYS.EXE)

screen.clear_raw!

for i -> [16<->48]
    screen.set(i, i, SCR.ENUM.PIXEL.BLACK)
.

screen.write("HELLO WORLD", 0, 0, SCR.ENUM.PIXEL.BLACK, font.miniwi)

screen.apply!